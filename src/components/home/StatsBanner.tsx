'use client';import { useState, useEffect } from 'react';import { motion } from 'framer-motion';import { FaUsers, FaStar, FaTrophy, FaCalendarDay } from 'react-icons/fa';type StatItemProps = {  icon: React.ReactNode;  value: string;  label: string;  delay: number;}const StatItem = ({ icon, value, label, delay }: StatItemProps) => {  const [isVisible, setIsVisible] = useState(false);  useEffect(() => {    const timer = setTimeout(() => setIsVisible(true), 100 + delay);    return () => clearTimeout(timer);  }, [delay]);  return (    <motion.div       className="flex flex-col items-center"      initial={{ opacity: 0, y: 20 }}      animate={isVisible ? { opacity: 1, y: 0 } : { opacity: 0, y: 20 }}      transition={{ duration: 0.7, ease: 'easeOut' }}    >      <div className="bg-primary/30 p-4 rounded-full mb-4 w-16 h-16 flex items-center justify-center text-2xl text-primary-light">        {icon}      </div>      <motion.div         className="text-4xl font-bold mb-2"        initial={{ opacity: 0 }}        animate={isVisible ? { opacity: 1 } : { opacity: 0 }}        transition={{ duration: 1, delay: 0.2 }}      >        {value}      </motion.div>      <div className="text-gray-300 text-center">{label}</div>    </motion.div>  );};export default function StatsBanner() {  return (    <section className="py-16 bg-card/50 relative overflow-hidden">      <div className="absolute top-0 left-0 w-full h-full bg-[url('/images/texture.png')] opacity-10 mix-blend-overlay"></div>      <div className="container mx-auto px-4">        <h2 className="text-3xl md:text-4xl font-bold mb-12 text-center">          Великая Отечественная Война <span className="text-primary-light">в цифрах</span>        </h2>        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">          <StatItem             icon={<FaCalendarDay />}             value="1418"             label="дней длилась война"            delay={0}          />          <StatItem             icon={<FaUsers />}             value="34 476 700"             label="человек участвовало в войне"            delay={200}          />          <StatItem             icon={<FaStar />}             value="11 657"             label="Героев Советского Союза"            delay={400}          />          <StatItem             icon={<FaTrophy />}             value="27 000 000+"             label="жизней унесла война"            delay={600}          />        </div>      </div>    </section>  );} 