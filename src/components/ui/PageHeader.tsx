'use client';import { motion } from 'framer-motion';type PageHeaderProps = {  title: string;  description?: string;  bgImage?: string;};export default function PageHeader({ title, description, bgImage = '/images/default-header.jpg' }: PageHeaderProps) {  return (    <section className="relative py-24 md:py-32">      {}      <div        className="absolute inset-0 bg-cover bg-center bg-no-repeat z-0 after:absolute after:inset-0 after:hero-gradient after:z-10"        style={{ backgroundImage: `url('${bgImage}')` }}      />      <div className="container-custom relative z-20">        <motion.div          className="max-w-3xl"          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ duration: 0.5 }}        >          <h1 className="text-4xl md:text-5xl font-bold mb-4 text-white">{title}</h1>          {description && (            <p className="text-xl text-gray-300">{description}</p>          )}        </motion.div>      </div>    </section>  );} 